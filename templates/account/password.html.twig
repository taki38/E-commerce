{% extends 'base.html.twig' %}

{% block header %}
    {% include ('parts/header.html.twig') %}
{% endblock %}

{% block body %}
        <div class="container mt-5 mb-5">
        {% if notification %}
            <div class="alert alert-info"> {{ notification }}</div>
        {% endif %}
        <a href="{{ path('account') }}" type="button" role="button" class="button button-info">Retour à mon compte</a>
        {{ form(form) }}
    </div>

{% endblock %}